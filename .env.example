# Google OAuth Configuration
# This is the only required environment variable
# Get your Client ID from Google Cloud Console: https://console.cloud.google.com/
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here.apps.googleusercontent.com

# All other configuration (spreadsheet, templates, folders) is created and stored automatically
# when users first log in via the Setup Wizard
